{
  "name": "elchocolate",
  "version": "0.0.0",
  "description": "A repository for chocolate-related products",
  "main": "server.js",
  "private": true,
  "dependencies": {
    "babel": "^4.4.5",
    "babelify": "^5.0.3",
    "browserify": "^9.0.3",
    "express": "^4.12.0",
    "react": "^0.12.2"
  },
  "devDependencies": {
    "nodemon": "^1.3.7",
    "watchify": "^2.4.0"
  },
  "scripts": {
    "start": "browserify app.jsx -t babelify --outfile app.js && node server.js",
    "watch": "watchify app.jsx -t babelify --outfile app.js & nodemon server.js"
  }
}
